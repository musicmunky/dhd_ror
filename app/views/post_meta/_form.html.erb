<%= form_for(@post_metum) do |f| %>
  <% if @post_metum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_metum.errors.count, "error") %> prohibited this post_metum from being saved:</h2>

      <ul>
      <% @post_metum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :meta_key %><br>
    <%= f.text_field :meta_key %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_area :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
